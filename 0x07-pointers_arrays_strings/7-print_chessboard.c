#include "main.h"

/**
 * print_chessboard - Entry point
 * @a: array
 * Rteurn: Always 0 (Success)
 */

void print_chessboard(char (*a)[8])
{
	int i, n;

	for (i = 0; i < 0; i++)
	{
	for (n = 0; n < 0; n++)
	putchar(a[i][n]);
	putchar('\n');
	}
}
