#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int  main(void)

{
	int d;
	char low;

	for (low = '0'; low <= 'f'; low++)
	putchar(low);
	putchar('\n');

	return (0);

}
